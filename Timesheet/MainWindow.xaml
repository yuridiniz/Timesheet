<Window x:Class="Timesheet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timesheet" Height="200" Width="300" WindowStyle="ToolWindow" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>

        </Grid.RowDefinitions>

        <StackPanel Name="btnEntradaContent" VerticalAlignment="Center">
            <Button Name="btnEntrada" Content="Entrada" Height="30" Width="100" Margin="10" VerticalAlignment="Center"/>
            <Button Name="btnSair" IsEnabled="False" Content="Saída" Height="30" Width="100" Margin="10" VerticalAlignment="Center"/>
        </StackPanel>
        <CheckBox Name="ckbConferir" Content="Conferir" HorizontalAlignment="Left" Margin="10,149,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
